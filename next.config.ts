import type { NextConfig } from "next";

// TODO: do a new port 1234
// TODO: matrix of reserved ports for Linqto apps
const nextConfig: NextConfig = {
  /* config options here */
};

export default nextConfig;
